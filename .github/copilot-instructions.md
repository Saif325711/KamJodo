# KamJodo Workspace Instructions

- Keep the Flutter user app inside `frontend/`.
- Keep the Flutter admin panel inside `admin/`.
- Keep the Node.js + Express backend inside `backend/`.
- Prefer small, focused edits that preserve the existing structure.
- For the frontend, keep the initial experience centered on the Instagram-style bottom navigation: Home, Feed, Message, Search, and Profile.
